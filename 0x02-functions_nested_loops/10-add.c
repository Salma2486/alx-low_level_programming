#include "main.h"
/**
 *add - function that adds two integers and returns the result
 *
 *@n: first intiger
 *@m: second intiger
 *Return: return sum
 */
int add(int n, int m)
{
	int sum = n + m;

	return (sum);
}
