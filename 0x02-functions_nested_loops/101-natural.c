#include "main.h"
/**
 *main - This is the entry point of the code
 *
 *Return:0 Success
 */
int main(void)
{
	int sum, num;

	for (num = 0; num < 1024; num++)
	{
		if (((num % 3 == 0) || (num % 5 == 0))
				sum += num;
	}
	printf("%d\n", num);
	return (0);
}
