#include <stdio.h>
/**
 *main - This is the entry point of the code
 *
 *Return:0 Success
 */
int main(void)
{
	int a;
	char n;

	for (a = 48; a < 58; a++)
		putchar(a);
	for (n = ; n <= 'f'; n++)
		putchar(n);
	putchar('\n');

	return (0);
}
